---
eleventyComputed:
  title: Tagged “{{ tag | titleify }}”
permalink: /{{ tag | slugify }}/
layout: layouts/home.njk
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
  addAllPagesToCollections: true
---

{% set uuids = featured | uuidsForTag(tag) %}
{% set posts = collections.all | postsForTag(tag) | pick(uuids) %}
{% include "tagList.njk" %}
